#!/usr/bin/env python3
"""type-annotated function"""
import math


def floor(n: float) -> int:
    """takes a float n and return a floor
    ARGS:
        n: a float
    returnS:
         a floor(n)
    """
    return (math.floor(n))
